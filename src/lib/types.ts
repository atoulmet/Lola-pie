export type SliceFormData = {
  id: string;
  percent: string;
  amount: string;
  label: string;
};

export type ChartSlice = {
  id?: string;
  percent: number;
  color?: string;
  label?: string;
};
